import { useState } from 'react';
function CreateWorkItemModal({ suggestion, onClose, onSave }) { const [title, setTitle] = useState(suggestion.title); const [ownerName, setOwnerName] = useState(suggestion.owner || ''); const [dueText, setDueText] = useState(suggestion.due || ''); const [dueAt, setDueAt] = useState(''); return <div className="work-item-modal"><form onSubmit={(event) => { event.preventDefault(); onSave({ title, ownerName, dueText, dueAt, category: suggestion.category, chatId: suggestion.source.chatId, sourceMessageId: suggestion.source._id }); }}><h2>Create tracker</h2><label>Title<input value={title} onChange={(event) => setTitle(event.target.value)} required /></label><label>Owner<input value={ownerName} onChange={(event) => setOwnerName(event.target.value)} /></label><label>Due details<input value={dueText} onChange={(event) => setDueText(event.target.value)} /></label><label>Due date<input type="date" value={dueAt} onChange={(event) => setDueAt(event.target.value)} /></label><p>Source: {suggestion.source.chatName}</p><button type="submit">Save tracker</button><button type="button" onClick={onClose}>Cancel</button></form></div>; }
export default CreateWorkItemModal;
